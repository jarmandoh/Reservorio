{
  "name": "reservorio-workspace",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.22.3",
    "pnpm": ">=9.15.0"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "format:check": "turbo run format:check",
    "format:fix": "turbo run format:fix"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
